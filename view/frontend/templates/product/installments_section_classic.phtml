<?php
/**
 * SanalPosPro Classic theme — full-width section wrapper variant.
 *
 * Same body as installments_classic.phtml but framed inside the dedicated
 * `product-full-width-section` so it sits in the Page Builder "Reviews"
 * area with a heading.
 *
 * @var \Sanalpospro\SanalPosPro\Block\Product\Installments $block
 */

if (!$block->isEnabled()) {
    return;
}

$activeFamilies = $block->getActiveCardFamilies();
if (empty($activeFamilies)) {
    return;
}
?>
<div class="product-full-width-section paythor-installments-section" id="paythor-installments">
    <h2 class="product-section-title sppro-installment-title"><?= $escaper->escapeHtml(__('Installments')) ?></h2>

    <div class="sppro-classic-wrapper" data-sanalpospro-wrapper>
        <div class="sppro-classic-container" data-sanalpospro-container>
            <?php foreach ($activeFamilies as $cardKey):
                $rows = $block->getFamilyRowsAll($cardKey);
                ?>
                <div class="sppro-classic-card" data-sanalpospro-card>
                    <table class="sppro-classic-table" data-sanalpospro-table>
                        <thead>
                            <tr>
                                <td colspan="3" class="sppro-classic-card-head">
                                    <img src="<?= $escaper->escapeUrl($block->getCardImageUrl($cardKey)) ?>"
                                         alt="<?= $escaper->escapeHtmlAttr($cardKey) ?>"
                                         class="sppro-card-image">
                                </td>
                            </tr>
                            <tr>
                                <th><?= $escaper->escapeHtml(__('Installment')) ?></th>
                                <th><?= $escaper->escapeHtml(__('Monthly Pay.')) ?></th>
                                <th><?= $escaper->escapeHtml(__('Total Amount')) ?></th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($rows as $row):
                                $months = (int)$row['months'];
                                $label  = sprintf(__('%d Installment'), $months);
                                ?>
                                <tr<?= $row['active'] ? '' : ' class="sppro-classic-row-empty"' ?>>
                                    <td><?= $escaper->escapeHtml($label) ?></td>
                                    <td><?= $row['active'] ? /* @noEscape */ $row['monthly'] : '-' ?></td>
                                    <td><?= $row['active'] ? /* @noEscape */ $row['total']   : '-' ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php endforeach; ?>
        </div>

        <div class="sppro-classic-note" data-sanalpospro-note>
            <p><?= $escaper->escapeHtml(__(
                'Installment amounts are estimated and may vary depending on the bank\'s campaigns and interest rates.'
            )) ?></p>
        </div>
    </div>
</div>
